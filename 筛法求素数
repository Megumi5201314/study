
#include<stdio.h>
#include<stdbool.h>
#include<math.h>
bool judge(int n)
{
    int i;
    for(i=2;i<=sqrt(n);i++)
        if(n%i==0)
        {
            return false;
            break;
        }
    return true;
}
